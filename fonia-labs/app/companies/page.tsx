import {companies,tiers} from "@/data/companies";import {CompanyCard} from "@/components/CompanyCard";import {PageHero} from "@/components/PageHero";
export default function CompaniesPage(){return <main><PageHero eyebrow="Ecosystem" title="Companies & Ventures" text="Fonia Labs houses a growing ecosystem of companies, ventures, platforms, and products built to solve real-world problems across different industries."/><section className="section"><div className="container">{tiers.map(tier=>{const items=companies.filter(company=>company.tier===tier);if(!items.length)return null;return <div key={tier} style={{marginBottom:54}}><div className="section-head"><div><div className="eyebrow">{tier}</div><h2>{tier}</h2></div><p>{tier==="Core Company"?"The core company supporting software production, digital services, product design, and client work.":tier==="Featured Company"?"Major companies positioned strongly in the Fonia Labs ecosystem.":tier==="Growth Company"?"Serious companies growing within the ecosystem around strong problem areas.":"Smaller, community-focused, creative, or experimental products under Fonia Labs."}</p></div><div className="grid cols-3">{items.map(company=><CompanyCard key={company.slug} company={company}/>)}</div></div>})}</div></section></main>}

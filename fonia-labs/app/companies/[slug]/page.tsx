import {notFound} from "next/navigation";import Link from "next/link";import {companies,getCompany} from "@/data/companies";
export function generateStaticParams(){return companies.map(company=>({slug:company.slug}))}
export function generateMetadata({params}:{params:{slug:string}}){const company=getCompany(params.slug);if(!company)return {};return{title:`${company.name} | Fonia Labs`,description:company.description}}
export default function CompanyPage({params}:{params:{slug:string}}){const company=getCompany(params.slug);if(!company)notFound();return <main><section className="company-hero"><div className="container company-layout"><div><div className="eyebrow">{company.category}</div><h1>{company.name}</h1><span className="badge">{company.badge}</span><p style={{fontSize:19}}>{company.tagline}</p><p>{company.description}</p><div className="actions">{company.url?<a className="btn primary" href={company.url} target="_blank" rel="noreferrer">Visit {company.name}</a>:null}<Link className="btn" href="/collaborate">Collaborate</Link></div></div><div className="mock-shot"><div><strong>{company.name}</strong><p>{company.status}</p><p>{company.badge}</p></div></div></div></section><section className="section"><div className="container split"><div><div className="eyebrow">Problem</div><h2>The problem {company.name} solves.</h2><p>{company.problem}</p></div><div><div className="eyebrow">Solution</div><h2>What {company.name} provides.</h2><p>{company.solution}</p></div></div></section><section className="section"><div className="container"><div className="section-head"><div><div className="eyebrow">Features</div><h2>Key features</h2></div><p>{company.name} is structured around useful product features that support its core mission.</p></div><div className="grid cols-3">{company.features.map(feature=><div className="card" key={feature}><h3>{feature}</h3><p>Part of the product direction and experience for {company.name}.</p></div>)}</div></div></section><section className="section"><div className="container split"><div><div className="eyebrow">Users</div><h2>Who {company.name} serves.</h2><ul className="list">{company.users.map(user=><li key={user}>{user}</li>)}</ul></div><div><div className="eyebrow">Screenshots</div><h2>Product experience</h2><p>Replace these placeholders with real smoke screenshots or screenshots from the live platform.</p><div className="grid cols-2">{company.screenshotSlots.slice(0,6).map(slot=><div className="mock-shot" style={{minHeight:155,borderRadius:20}} key={slot}><div><strong style={{fontSize:18}}>{slot}</strong><p>{company.name}</p></div></div>)}</div></div></div></section>{company.note?<section className="section"><div className="container warning-note"><h3>Important note</h3><p>{company.note}</p></div></section>:null}<section className="section"><div className="container split"><div><div className="eyebrow">Roadmap</div><h2>What comes next for {company.name}</h2><ul className="list">{company.roadmap.map(item=><li key={item}>{item}</li>)}</ul></div><div className="card"><div className="eyebrow">Collaborate</div><h2>Test or collaborate with {company.name}</h2><p>{company.collaboration}</p><div className="actions"><Link className="btn primary" href="/collaborate">Start a Conversation</Link><Link className="btn" href="/companies">Back to Companies</Link></div></div></div></section></main>}
